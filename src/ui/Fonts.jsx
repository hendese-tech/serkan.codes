import { Inter, Manrope } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'], display: 'swap', variable: '--font-in'});

export const manrope = Manrope({subsets: ['latin'], display: 'swap', variable: '--font-mr', weight: ['500', '700']})